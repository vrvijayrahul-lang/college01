import { GenericFeature } from "@/components/generic-feature";

export default function Page() {
  return (
    <GenericFeature
      title="Chat with Faculty"
      description="Secure messaging between students, parents, and faculty."
    />
  );
}
