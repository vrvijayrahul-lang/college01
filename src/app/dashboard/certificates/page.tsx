import { GenericFeature } from "@/components/generic-feature";

export default function Page() {
  return (
    <GenericFeature
      title="Certificates"
      description="Download course and achievement certificates with QR verification."
    />
  );
}
