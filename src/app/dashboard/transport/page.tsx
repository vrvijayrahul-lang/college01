import { GenericFeature } from "@/components/generic-feature";

export default function Page() {
  return (
    <GenericFeature
      title="Transport"
      description="View routes, bus passes, and transport fees."
    />
  );
}
